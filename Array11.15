#include <iostream>
using namespace std;

int num[10];
int timp = 0;
int arr;
int main() {
	cout << "Enter ten numbers" << endl;

	cin >> num[0];
	cin >> num[1];
	cin >> num[2];
	cin >> num[3];
	cin >> num[4];
	cin >> num[5];
	cin >> num[6];
	cin >> num[7];
	cin >> num[8];
	cin >> num[9];

	for (int i = 0; i < 10; i++) {
		if (num[i] > 10) {
			timp = num[i];
			arr = i;
		}

	}
		cout << "The biggest number is:" << timp << endl;
		cout << "It was in index:" << arr << endl;
	
}
